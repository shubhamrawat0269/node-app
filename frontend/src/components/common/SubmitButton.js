export const SubmitButton = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "end",
        padding: "8px",
      }}
    >
      <button className="submit-btn" type="submit">
        Submit
      </button>
    </div>
  );
};
